












Substring Attempt #1
 if(str.length > num) {
   temp1 = str.slice(0,num-3);
   temp2 = temp1.concat("...");
   return temp2;
 } else {
   if(str.length < num) {
     temp2 = str.concat("...");
     return temp2;
   }

}
